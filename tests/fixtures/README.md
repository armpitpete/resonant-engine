# Test fixtures

M0 canonical seed: `777`.

Fixtures are versioned with the repository. Future audio regressions must record the core version, sample rate, block size, event sequence, seed, tolerance and expected measurements. Bit-identical floating-point output is not a cross-platform requirement unless a fixture explicitly says so.
